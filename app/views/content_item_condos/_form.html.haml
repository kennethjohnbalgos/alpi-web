= form_for @content_item_condo do |f|
  -if @content_item_condo.errors.any?
    #error_explanation
      %h2= "#{pluralize(@content_item_condo.errors.count, "error")} prohibited this content_item_condo from being saved:"
      %ul
        - @content_item_condo.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    %br/
    = f.text_field :title
  .field
    = f.cktext_area :context, :width => 770, :height => 450
  .actions
    = f.submit 'Save'
