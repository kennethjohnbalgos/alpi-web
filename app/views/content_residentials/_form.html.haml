= form_for @content_residential do |f|
  -if @content_residential.errors.any?
    #error_explanation
      %h2= "#{pluralize(@content_residential.errors.count, "error")} prohibited this content_residential from being saved:"
      %ul
        - @content_residential.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    %br/
    = f.text_field :title
          
  .field
    = f.cktext_area :context
  %br/
  .actions
    = f.submit 'Save Changes'
