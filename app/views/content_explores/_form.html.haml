= form_for @content_explore do |f|
  -if @content_explore.errors.any?
    #error_explanation
      %h2= "#{pluralize(@content_explore.errors.count, "error")} prohibited this content_explore from being saved:"
      %ul
        - @content_explore.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    %br/
    = f.text_field :title
          
  .field
    = f.cktext_area :context
  %br/
  .actions
    = f.submit 'Save Changes'
